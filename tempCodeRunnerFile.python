rowIndex = 3

def getRow(rowIndex: int) -> list[int]:
    pascal = [[1], [1,1]]
    if rowIndex < 2:
        return pascal[rowIndex]
    pascal = [1]
    for i in range (rowIndex-1):
        pascal.append(rowIndex)
    pascal.append(1)
    return pascal


print(getRow(rowIndex))